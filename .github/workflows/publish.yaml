name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  actions: read

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/build.yaml@main
    secrets:
      token: ${{secrets.WAPWAGNER_TOKEN}}
