name: Publish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  publish:
    uses: thepwagner-org/actions/.github/workflows/publish.yaml@aa7a4cf9d36faf33af77b64921afe7a77b2427fe # v2.3.0
    secrets:
      token: ${{secrets.GITHUB_TOKEN}}
