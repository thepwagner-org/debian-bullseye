name: Check
on:
  workflow_dispatch:
  schedule:
    - cron:  '5 4 * * *'

permissions:
  contents: write
  packages: read
  pull-requests: write

jobs:
  check:
    uses: thepwagner-org/actions/.github/workflows/check.yaml@004a11d0b52f514d9907b4c8ddfd814414bb316e # v2.4.0
    secrets:
      not_github_token: ${{secrets.WAPWAGNER_TOKEN}}
