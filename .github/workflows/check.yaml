name: Check
on:
  workflow_dispatch:
  schedule:
    - cron:  '5 4 * * *'

permissions:
  contents: write
  packages: read
  pull-requests: write

jobs:
  check:
    uses: thepwagner-org/actions/.github/workflows/check.yaml@aa08dc4b97008eafd33fa593024663ef59693e71 # v2.7.0
    secrets:
      not_github_token: ${{secrets.WAPWAGNER_TOKEN}}
