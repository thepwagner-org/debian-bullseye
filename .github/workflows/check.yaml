name: Check
on:
  workflow_dispatch:
  schedule:
    - cron:  '5 4 * * *'

permissions:
  contents: write
  packages: read
  pull-requests: write

jobs:
  check:
    uses: thepwagner-org/actions/.github/workflows/check.yaml@552d802cd0888d34410e1c4731e3ee559a18c5c2 # tag=v2.2.2
    secrets:
      not_github_token: ${{secrets.WAPWAGNER_TOKEN}}
