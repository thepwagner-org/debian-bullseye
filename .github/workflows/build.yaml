name: Build
on:
  push:

permissions:
  contents: write 
  actions: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: thepwagner-org/.github/.github/workflows/build.yaml@main
        with:
          image: meow
